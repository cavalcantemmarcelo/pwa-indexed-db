{
  "name": "idb-user-main",
  "version": "1.0.0",
  "description": "## Instalação: Para instalar o projeto é necessário ter o Node.js (12.22ˆ) Abra o terminal na pasta do projeto e execute o comando: > npm install",
  "main": "jest.config.js",
  "scripts": {
    "test": "jest --verbose",
    "scss": "sass scss/style.scss:css/style.css",
    "watch": "nodemon -e scss -x \"npm run scss\"",
    "start": "npm run scss && node server.js && npm run watch"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "node-loader": "^2.0.0"
  },
  "dependencies": {
    "body-parser": "^1.20.2",
    "express": "^4.18.2",
    "npm-watch": "^0.11.0"
  }
}
